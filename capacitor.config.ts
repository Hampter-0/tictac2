import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nl.hampternom.tictac2',
  appName: 'tictac2',
  webDir: 'dist/tictac2/browser'
};

export default config;
